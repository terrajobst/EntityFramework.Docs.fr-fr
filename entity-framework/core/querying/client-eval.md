---
title: Évaluation client vs serveur - EF Core
author: smitpatel
ms.date: 10/03/2019
ms.assetid: 8b6697cc-7067-4dc2-8007-85d80503d123
uid: core/querying/client-eval
ms.openlocfilehash: e01bd146c4dfe7a8d36b641cb52ae366fddd8239
ms.sourcegitcommit: 9b562663679854c37c05fca13d93e180213fb4aa
ms.translationtype: MT
ms.contentlocale: fr-FR
ms.lasthandoff: 04/07/2020
ms.locfileid: "78417759"
---
# <a name="client-vs-server-evaluation"></a><span data-ttu-id="4656e-102">Évaluation du client par rapport au serveur</span><span class="sxs-lookup"><span data-stu-id="4656e-102">Client vs. Server Evaluation</span></span>

<span data-ttu-id="4656e-103">En règle générale, Entity Framework Core tente d’évaluer autant que possible une requête sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="4656e-103">As a general rule, Entity Framework Core attempts to evaluate a query on the server as much as possible.</span></span> <span data-ttu-id="4656e-104">EF Core convertit certaines parties de la requête en paramètres qu’elle peut évaluer du côté du client.</span><span class="sxs-lookup"><span data-stu-id="4656e-104">EF Core converts parts of the query into parameters, which it can evaluate on the client side.</span></span> <span data-ttu-id="4656e-105">Le reste de la requête (ainsi que les paramètres générés) est remis au fournisseur de base de données pour déterminer la requête de base de données équivalente à évaluer sur le serveur.</span><span class="sxs-lookup"><span data-stu-id="4656e-105">The rest of the query (along with the generated parameters) is given to the database provider to determine the equivalent database query to evaluate on the server.</span></span> <span data-ttu-id="4656e-106">EF Core soutient l’évaluation partielle des clients dans la `Select()`projection de haut niveau (essentiellement, le dernier appel à ).</span><span class="sxs-lookup"><span data-stu-id="4656e-106">EF Core supports partial client evaluation in the top-level projection (essentially, the last call to `Select()`).</span></span> <span data-ttu-id="4656e-107">Si la projection de haut niveau de la requête ne peut pas être traduite sur le serveur, EF Core récupérera toutes les données requises du serveur et évaluera les parties restantes de la requête sur le client.</span><span class="sxs-lookup"><span data-stu-id="4656e-107">If the top-level projection in the query can't be translated to the server, EF Core will fetch any required data from the server and evaluate remaining parts of the query on the client.</span></span> <span data-ttu-id="4656e-108">Si EF Core détecte une expression, dans n’importe quel endroit autre que la projection de haut niveau, qui ne peut pas être traduite sur le serveur, alors elle lance une exception de temps d’exécution.</span><span class="sxs-lookup"><span data-stu-id="4656e-108">If EF Core detects an expression, in any place other than the top-level projection, which can't be translated to the server, then it throws a runtime exception.</span></span> <span data-ttu-id="4656e-109">Découvrez [comment fonctionne la requête](xref:core/querying/how-query-works) pour comprendre comment EF Core détermine ce qui ne peut pas être traduit en serveur.</span><span class="sxs-lookup"><span data-stu-id="4656e-109">See [how query works](xref:core/querying/how-query-works) to understand how EF Core determines what can't be translated to server.</span></span>

> [!NOTE]
> <span data-ttu-id="4656e-110">Avant la version 3.0, Entity Framework Core a soutenu l’évaluation des clients n’importe où dans la requête.</span><span class="sxs-lookup"><span data-stu-id="4656e-110">Prior to version 3.0, Entity Framework Core supported client evaluation anywhere in the query.</span></span> <span data-ttu-id="4656e-111">Pour plus d’informations, voir la [section versions précédentes](#previous-versions).</span><span class="sxs-lookup"><span data-stu-id="4656e-111">For more information, see the [previous versions section](#previous-versions).</span></span>

> [!TIP]
> <span data-ttu-id="4656e-112">Vous pouvez afficher cet [exemple](https://github.com/dotnet/EntityFramework.Docs/tree/master/samples/core/Querying) sur GitHub.</span><span class="sxs-lookup"><span data-stu-id="4656e-112">You can view this article's [sample](https://github.com/dotnet/EntityFramework.Docs/tree/master/samples/core/Querying) on GitHub.</span></span>

## <a name="client-evaluation-in-the-top-level-projection"></a><span data-ttu-id="4656e-113">Évaluation des clients dans la projection de haut niveau</span><span class="sxs-lookup"><span data-stu-id="4656e-113">Client evaluation in the top-level projection</span></span>

<span data-ttu-id="4656e-114">Dans l’exemple suivant, une méthode d’aide est utilisée pour normaliser les URL pour les blogs, qui sont retournés à partir d’une base de données SQL Server.</span><span class="sxs-lookup"><span data-stu-id="4656e-114">In the following example, a helper method is used to standardize URLs for blogs, which are returned from a SQL Server database.</span></span> <span data-ttu-id="4656e-115">Étant donné que le fournisseur de serveurs SQL n’a aucune idée de la façon dont cette méthode est implémentée, il n’est pas possible de la traduire en SQL.</span><span class="sxs-lookup"><span data-stu-id="4656e-115">Since the SQL Server provider has no insight into how this method is implemented, it isn't possible to translate it into SQL.</span></span> <span data-ttu-id="4656e-116">Tous les autres aspects de la requête sont évalués `URL` dans la base de données, mais le passage de la méthode retournée est fait sur le client.</span><span class="sxs-lookup"><span data-stu-id="4656e-116">All other aspects of the query are evaluated in the database, but passing the returned `URL` through this method is done on the client.</span></span>

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ClientProjection)]

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ClientMethod)]

## <a name="unsupported-client-evaluation"></a><span data-ttu-id="4656e-117">Évaluation non étayée par les clients</span><span class="sxs-lookup"><span data-stu-id="4656e-117">Unsupported client evaluation</span></span>

<span data-ttu-id="4656e-118">Bien que l’évaluation des clients soit utile, elle peut parfois entraîner de mauvaises performances.</span><span class="sxs-lookup"><span data-stu-id="4656e-118">While client evaluation is useful, it can result in poor performance sometimes.</span></span> <span data-ttu-id="4656e-119">Considérez la requête suivante, dans laquelle la méthode d’aide est maintenant utilisée dans un filtre où.</span><span class="sxs-lookup"><span data-stu-id="4656e-119">Consider the following query, in which the helper method is now used in a where filter.</span></span> <span data-ttu-id="4656e-120">Étant donné que le filtre ne peut pas être appliqué dans la base de données, toutes les données doivent être extraites dans la mémoire pour appliquer le filtre sur le client.</span><span class="sxs-lookup"><span data-stu-id="4656e-120">Because the filter can't be applied in the database, all the data needs to be pulled into memory to apply the filter on the client.</span></span> <span data-ttu-id="4656e-121">Sur la base du filtre et de la quantité de données sur le serveur, l’évaluation des clients pourrait entraîner de mauvaises performances.</span><span class="sxs-lookup"><span data-stu-id="4656e-121">Based on the filter and the amount of data on the server, client evaluation could result in poor performance.</span></span> <span data-ttu-id="4656e-122">Ainsi, Entity Framework Core bloque une telle évaluation des clients et jette une exception de temps d’exécution.</span><span class="sxs-lookup"><span data-stu-id="4656e-122">So Entity Framework Core blocks such client evaluation and throws a runtime exception.</span></span>

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ClientWhere)]

## <a name="explicit-client-evaluation"></a><span data-ttu-id="4656e-123">Évaluation explicite des clients</span><span class="sxs-lookup"><span data-stu-id="4656e-123">Explicit client evaluation</span></span>

<span data-ttu-id="4656e-124">Vous devrez peut-être entrer en vigueur explicitement dans l’évaluation des clients dans certains cas comme</span><span class="sxs-lookup"><span data-stu-id="4656e-124">You may need to force into client evaluation explicitly in certain cases like following</span></span>

- <span data-ttu-id="4656e-125">La quantité de données est faible de sorte que l’évaluation sur le client n’entraîne pas une pénalité de performance énorme.</span><span class="sxs-lookup"><span data-stu-id="4656e-125">The amount of data is small so that evaluating on the client doesn't incur a huge performance penalty.</span></span>
- <span data-ttu-id="4656e-126">L’opérateur LINQ utilisé n’a pas de traduction côté serveur.</span><span class="sxs-lookup"><span data-stu-id="4656e-126">The LINQ operator being used has no server-side translation.</span></span>

<span data-ttu-id="4656e-127">Dans de tels cas, vous pouvez explicitement `AsEnumerable` opter pour l’évaluation des clients en appelant des méthodes comme ou `ToList` (ou`AsAsyncEnumerable` `ToListAsync` pour async).</span><span class="sxs-lookup"><span data-stu-id="4656e-127">In such cases, you can explicitly opt into client evaluation by calling methods like `AsEnumerable` or `ToList` (`AsAsyncEnumerable` or `ToListAsync` for async).</span></span> <span data-ttu-id="4656e-128">En `AsEnumerable` vous utilisant serait streaming les `ToList` résultats, mais en utilisant cause de tamponnage en créant une liste, qui prend également la mémoire supplémentaire.</span><span class="sxs-lookup"><span data-stu-id="4656e-128">By using `AsEnumerable` you would be streaming the results, but using `ToList` would cause buffering by creating a list, which also takes additional memory.</span></span> <span data-ttu-id="4656e-129">Bien que si vous énumérez plusieurs fois, puis stocker des résultats dans une liste aide plus car il n’y a qu’une seule requête à la base de données.</span><span class="sxs-lookup"><span data-stu-id="4656e-129">Though if you're enumerating multiple times, then storing results in a list helps more since there's only one query to the database.</span></span> <span data-ttu-id="4656e-130">Selon l’utilisation particulière, vous devez évaluer quelle méthode est la plus utile pour le cas.</span><span class="sxs-lookup"><span data-stu-id="4656e-130">Depending on the particular usage, you should evaluate which method is more useful for the case.</span></span>

[!code-csharp[Main](../../../samples/core/Querying/ClientEval/Sample.cs#ExplicitClientEval)]

## <a name="potential-memory-leak-in-client-evaluation"></a><span data-ttu-id="4656e-131">Fuite de mémoire potentielle dans l’évaluation du client</span><span class="sxs-lookup"><span data-stu-id="4656e-131">Potential memory leak in client evaluation</span></span>

<span data-ttu-id="4656e-132">Étant donné que la traduction et la compilation des requêtes sont coûteuses, EF Core cache le plan de requête compilé.</span><span class="sxs-lookup"><span data-stu-id="4656e-132">Since query translation and compilation are expensive, EF Core caches the compiled query plan.</span></span> <span data-ttu-id="4656e-133">Le délégué mis en cache peut utiliser le code client tout en effectuant l’évaluation des clients de la projection de haut niveau.</span><span class="sxs-lookup"><span data-stu-id="4656e-133">The cached delegate may use client code while doing client evaluation of top-level projection.</span></span> <span data-ttu-id="4656e-134">EF Core génère des paramètres pour les parties évaluées par le client de l’arbre et réutilise le plan de requête en remplaçant les valeurs de paramètres.</span><span class="sxs-lookup"><span data-stu-id="4656e-134">EF Core generates parameters for the client-evaluated parts of the tree and reuses the query plan by replacing the parameter values.</span></span> <span data-ttu-id="4656e-135">Mais certaines constantes dans l’arbre d’expression ne peuvent pas être converties en paramètres.</span><span class="sxs-lookup"><span data-stu-id="4656e-135">But certain constants in the expression tree can't be converted into parameters.</span></span> <span data-ttu-id="4656e-136">Si le délégué mis en cache contient de telles constantes, ces objets ne peuvent pas être des ordures collectées puisqu’ils sont encore référencés.</span><span class="sxs-lookup"><span data-stu-id="4656e-136">If the cached delegate contains such constants, then those objects can't be garbage collected since they're still being referenced.</span></span> <span data-ttu-id="4656e-137">Si un tel objet contient un DbContext ou d’autres services en elle, alors il pourrait provoquer l’utilisation de la mémoire de l’application à croître au fil du temps.</span><span class="sxs-lookup"><span data-stu-id="4656e-137">If such an object contains a DbContext or other services in it, then it could cause the memory usage of the app to grow over time.</span></span> <span data-ttu-id="4656e-138">Ce comportement est généralement le signe d’une fuite de mémoire.</span><span class="sxs-lookup"><span data-stu-id="4656e-138">This behavior is generally a sign of a memory leak.</span></span> <span data-ttu-id="4656e-139">EF Core lance une exception chaque fois qu’elle rencontre des constantes d’un type qui ne peut pas être cartographié à l’aide du fournisseur de base de données actuel.</span><span class="sxs-lookup"><span data-stu-id="4656e-139">EF Core throws an exception whenever it comes across constants of a type that can't be mapped using current database provider.</span></span> <span data-ttu-id="4656e-140">Les causes communes et leurs solutions sont les suivantes :</span><span class="sxs-lookup"><span data-stu-id="4656e-140">Common causes and their solutions are as follows:</span></span>

- <span data-ttu-id="4656e-141">**À l’aide d’une méthode d’instance**: Lorsque vous utilisez des méthodes d’instance dans une projection client, l’arbre d’expression contient une constante de l’instance.</span><span class="sxs-lookup"><span data-stu-id="4656e-141">**Using an instance method**: When using instance methods in a client projection, the expression tree contains a constant of the instance.</span></span> <span data-ttu-id="4656e-142">Si votre méthode n’utilise aucune donnée de l’instance, envisagez de rendre la méthode statique.</span><span class="sxs-lookup"><span data-stu-id="4656e-142">If your method doesn't use any data from the instance, consider making the method static.</span></span> <span data-ttu-id="4656e-143">Si vous avez besoin de données d’instance dans le corps de la méthode, puis passer les données spécifiques comme un argument à la méthode.</span><span class="sxs-lookup"><span data-stu-id="4656e-143">If you need instance data in the method body, then pass the specific data as an argument to the method.</span></span>
- <span data-ttu-id="4656e-144">**Passer des arguments constants à la** `this` méthode : Ce cas se pose généralement en utilisant dans un argument à la méthode client.</span><span class="sxs-lookup"><span data-stu-id="4656e-144">**Passing constant arguments to method**: This case arises generally by using `this` in an argument to client method.</span></span> <span data-ttu-id="4656e-145">Envisagez de diviser l’argument à plusieurs arguments scalaires, qui peuvent être cartographiés par le fournisseur de base de données.</span><span class="sxs-lookup"><span data-stu-id="4656e-145">Consider splitting the argument in to multiple scalar arguments, which can be mapped by the database provider.</span></span>
- <span data-ttu-id="4656e-146">**Autres constantes**: Si une constante est à l’étude dans tous les autres cas, vous pouvez évaluer si la constante est nécessaire dans le traitement.</span><span class="sxs-lookup"><span data-stu-id="4656e-146">**Other constants**: If a constant is come across in any other case, then you can evaluate whether the constant is needed in processing.</span></span> <span data-ttu-id="4656e-147">S’il est nécessaire d’avoir la constante, ou si vous ne pouvez pas utiliser une solution à partir des cas ci-dessus, puis créer une variable locale pour stocker la valeur et utiliser la variable locale dans la requête.</span><span class="sxs-lookup"><span data-stu-id="4656e-147">If it's necessary to have the constant, or if you can't use a solution from the above cases, then create a local variable to store the value and use local variable in the query.</span></span> <span data-ttu-id="4656e-148">EF Core convertira la variable locale en paramètre.</span><span class="sxs-lookup"><span data-stu-id="4656e-148">EF Core will convert the local variable into a parameter.</span></span>

## <a name="previous-versions"></a><span data-ttu-id="4656e-149">Versions antérieures</span><span class="sxs-lookup"><span data-stu-id="4656e-149">Previous versions</span></span>

<span data-ttu-id="4656e-150">La section suivante s’applique aux versions EF Core avant 3.0.</span><span class="sxs-lookup"><span data-stu-id="4656e-150">The following section applies to EF Core versions before 3.0.</span></span>

<span data-ttu-id="4656e-151">Les anciennes versions EF Core ont soutenu l’évaluation des clients dans n’importe quelle partie de la requête- et pas seulement la projection de haut niveau.</span><span class="sxs-lookup"><span data-stu-id="4656e-151">Older EF Core versions supported client evaluation in any part of the query--not just the top-level projection.</span></span> <span data-ttu-id="4656e-152">C’est pourquoi les requêtes similaires à celles affichées dans la section [d’évaluation des clients non pris en compte](#unsupported-client-evaluation) ont fonctionné correctement.</span><span class="sxs-lookup"><span data-stu-id="4656e-152">That's why queries similar to one posted under the [Unsupported client evaluation](#unsupported-client-evaluation) section worked correctly.</span></span> <span data-ttu-id="4656e-153">Étant donné que ce comportement pourrait causer des problèmes de performance inaperçus, EF Core a enregistré un avertissement d’évaluation client.</span><span class="sxs-lookup"><span data-stu-id="4656e-153">Since this behavior could cause unnoticed performance issues, EF Core logged a client evaluation warning.</span></span> <span data-ttu-id="4656e-154">Pour plus d’informations sur l’affichage de la sortie d’enregistrement, voir [Logging](xref:core/miscellaneous/logging).</span><span class="sxs-lookup"><span data-stu-id="4656e-154">For more information on viewing logging output, see [Logging](xref:core/miscellaneous/logging).</span></span>

<span data-ttu-id="4656e-155">Optionnellement EF Core vous a permis de modifier le comportement par défaut pour lancer une exception ou ne rien faire lors de l’évaluation du client (sauf dans la projection).</span><span class="sxs-lookup"><span data-stu-id="4656e-155">Optionally EF Core allowed you to change the default behavior to either throw an exception or do nothing when doing client evaluation (except for in the projection).</span></span> <span data-ttu-id="4656e-156">Le comportement de lancer d’exception le rendrait semblable au comportement dans 3.0.</span><span class="sxs-lookup"><span data-stu-id="4656e-156">The exception throwing behavior would make it similar to the behavior in 3.0.</span></span> <span data-ttu-id="4656e-157">Pour modifier le comportement, vous devez configurer des avertissements `DbContext.OnConfiguring`tout en `Startup.cs` configurant les options pour votre contexte - généralement en , ou si vous utilisez ASP.NET Core.</span><span class="sxs-lookup"><span data-stu-id="4656e-157">To change the behavior, you need to configure warnings while setting up the options for your context - typically in `DbContext.OnConfiguring`, or in `Startup.cs` if you're using ASP.NET Core.</span></span>

```csharp
protected override void OnConfiguring(DbContextOptionsBuilder optionsBuilder)
{
    optionsBuilder
        .UseSqlServer(@"Server=(localdb)\mssqllocaldb;Database=EFQuerying;Trusted_Connection=True;")
        .ConfigureWarnings(warnings => warnings.Throw(RelationalEventId.QueryClientEvaluationWarning));
}
```
